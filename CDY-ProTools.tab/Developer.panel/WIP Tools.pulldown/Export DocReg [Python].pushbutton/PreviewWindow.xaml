<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Revision Preview" Height="500" Width="1100" WindowStartupLocation="CenterScreen">
    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
            <Label Content="Naming Protocol:" VerticalAlignment="Center"/>
            <ComboBox x:Name="NamingProtocolCombo" Width="260" Margin="8,0,0,0"/>
            <Label Content="(Preview shows first rows)" VerticalAlignment="Center" Margin="12,0,0,0"/>
        </StackPanel>

        <DataGrid x:Name="PreviewDataGrid" Grid.Row="1" AutoGenerateColumns="False" IsReadOnly="True"
                  HeadersVisibility="Column" CanUserAddRows="False" CanUserResizeColumns="True"
                  HorizontalScrollBarVisibility="Auto"/>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0">
            <Button x:Name="OkButton" Content="Export" Width="90" Margin="0,0,8,0"/>
            <Button x:Name="CancelButton" Content="Cancel" Width="90"/>
        </StackPanel>
    </Grid>
</Window>
