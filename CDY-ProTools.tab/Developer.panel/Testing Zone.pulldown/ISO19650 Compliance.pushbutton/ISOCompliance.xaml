<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="ISO 19650 Compliance"
        Height="600"
        Width="1500"
        WindowStartupLocation="CenterScreen">

    <Grid Margin="10">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- TOP BAR -->
        <Grid Grid.Row="0" Margin="0,0,0,10">

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="420"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <TextBlock Text="Naming Format:"
                       VerticalAlignment="Center"
                       Margin="0,0,10,0"/>

            <ComboBox x:Name="NamingCombo"
                      Grid.Column="1"
                      Width="400"
                      Height="28"/>

            <CheckBox x:Name="HideNoRevisionCheck"
                      Grid.Column="2"
                      Content="Hide sheets with no revision"
                      Margin="20,0,0,0"
                      VerticalAlignment="Center"/>

            <TextBlock Grid.Column="3"
                       Text= "This tool supports QA processes and does not replace professional responsibility for model or document compliance."
                       VerticalAlignment="Center"
                       HorizontalAlignment="Right"
                       Margin="20,0,0,0"
                       Foreground="#8B0000"
                       FontWeight="Bold"
                       FontStyle="Italic"/>

        </Grid>

        <!-- RESULTS GRID -->
        <DataGrid x:Name="ResultsGrid"
                  Grid.Row="1"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  HeadersVisibility="Column">

            <DataGrid.Resources>
                <Style TargetType="TextBlock">
                    <Setter Property="TextWrapping" Value="Wrap"/>
                    <Setter Property="VerticalAlignment" Value="Center"/>
                </Style>
            </DataGrid.Resources>

            <DataGrid.Columns>

                <DataGridTextColumn Header="Sheet"
                                    Binding="{Binding Sheet}"
                                    Width="50"/>

                <DataGridTextColumn Header="Filename"
                                    Binding="{Binding Name}"
                                    Width="600"/>

                <DataGridTextColumn Header="Status"
                                    Binding="{Binding Status}"
                                    Width="70"/>

                <DataGridTextColumn Header="Message"
                                    Binding="{Binding Message}"
                                    Width="*">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="TextWrapping" Value="Wrap"/>
                            <Setter Property="VerticalAlignment" Value="Center"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>

            </DataGrid.Columns>

            <DataGrid.RowStyle>
                <Style TargetType="DataGridRow">

                    <Setter Property="Background" Value="White"/>
                    <Setter Property="MinHeight" Value="28"/>

                    <Style.Triggers>

                        <DataTrigger Binding="{Binding Status}" Value="OK">
                            <Setter Property="Background" Value="#DFF5E1"/>
                        </DataTrigger>

                        <DataTrigger Binding="{Binding Status}" Value="WARNING">
                            <Setter Property="Background" Value="#FFF4CC"/>
                        </DataTrigger>

                        <DataTrigger Binding="{Binding Status}" Value="REVIEW">
                            <Setter Property="Background" Value="#FFE6B3"/>
                        </DataTrigger>

                        <DataTrigger Binding="{Binding Status}" Value="ERROR">
                            <Setter Property="Background" Value="#F8D7DA"/>
                        </DataTrigger>

                    </Style.Triggers>

                </Style>
            </DataGrid.RowStyle>

        </DataGrid>

    </Grid>
</Window>